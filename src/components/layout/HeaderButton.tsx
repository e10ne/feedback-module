import { Button } from "@chakra-ui/react";

interface HeaderButtonProps {}

const HeaderButton: React.FC<HeaderButtonProps> = ({}) => {
  return <Button bgColor={"button"}>Hello</Button>;
};

export default HeaderButton;
