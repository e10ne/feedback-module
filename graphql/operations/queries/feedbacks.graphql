query feedbacks($categoryId: Int, $text: String) {
  feedbacks(categoryId: $categoryId, text: $text) {
    id
    title
    description
    create_date
    category {
      id
      title
    }
  }
}
