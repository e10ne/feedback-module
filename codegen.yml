overwrite: true
schema: "http://localhost:3000/api/graphql"
documents: "graphql/opertations/**/*.graphql"
generates:
  src/graphql/graphql.tsx:
    config:
      withHooks: true
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
