query ArchivedFeedbacks($cursor: Int) {
  archivedFeedbacks(cursor: $cursor) {
    feedbacks {
      id
      title
      description
      create_date
      category {
        id
        title
      }
    }
    hasMore
    nextCursor
  }
}
