query ArchivedFeedbacks($cursor: Int) {
  archivedFeedbacks(cursor: $cursor) {
    ArchivedFeedbacks {
      id
      title
      description
      create_date
      category {
        id
        title
      }
    }
    hasMore
    nextCursor
  }
}
